import { Animated } from "../components";

export * from "./core";
export const {
    View,
    Text,
    Image,
    ScriptView,
    RenderView,
    CanvasView,
    ScrollView,
    FlatList,
    createAnimatedComponent,
    useSharedValue,
    useDerivedValue,
    useAnimatedStyle,
    useAnimatedProps,
    interpolate,
    interpolateColor,
    withTiming,
    withSpring,
    withDelay,
    withSequence,
    cancelAnimation,
    runOnUI,
    playbackClock,
} = Animated;

export default Animated;

